{
  "name": "mahalaxmi-gems",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace frontend",
    "dev:server": "npm run dev --workspace backend",
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev:client\" \"npm run dev:server\"",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "seed:catalog": "npm run seed:catalog --workspace backend --"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
